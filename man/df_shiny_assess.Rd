% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_assess.R
\name{df_shiny_assess}
\alias{df_shiny_assess}
\title{Launch Shiny App for Assessing Paper Relevance}
\usage{
df_shiny_assess(launch.browser = TRUE, ...)
}
\arguments{
\item{launch.browser}{Logical, passed to \code{shiny::runApp}. Whether to launch
the app in the default browser.}

\item{...}{Additional arguments passed to \code{shiny::runApp} (e.g., \code{port}, \code{host}).}
}
\value{
Does not return a value; runs the Shiny application.
}
\description{
Launches a simple Shiny application to assist with the assessment phase of
the DataFindR workflow for a single paper. It allows fetching metadata,
viewing the assessment prompt, triggering an LLM assessment via
\code{DataFindR::df_assess_relevance}, and viewing the results.
}
\details{
Before running the app:
\itemize{
\item Ensure the \code{metawoRld} package is installed and accessible.
\item Ensure the target \code{metawoRld} project directory exists and contains a valid \verb{_metawoRld.yml}.
\item If triggering API calls, ensure the relevant API key (e.g., \code{OPENAI_API_KEY}) is set in your environment (see \code{.get_api_key} documentation).
\item The \code{rclipboard} package is needed for the 'Copy Prompt' button.
}
}
